# infrastructure/traefik/dynamic/otp-rate-limit.yml
# مطابق سند ۱۵: OTP محدود به ۳ درخواست در دقیقه

http:
  middlewares:
    otp-strict-ratelimit:
      rateLimit:
        average: 3
        period: 1m
        burst: 5
        sourceCriterion:
          requestHeaderName: X-Forwarded-For
