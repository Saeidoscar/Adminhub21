import type { CommentRow } from "../content"
import {
  listComments,
  getComment,
  createComment,
  deleteComment,
} from "../content"

export type { CommentRow }
export { listComments, getComment, createComment, deleteComment }
